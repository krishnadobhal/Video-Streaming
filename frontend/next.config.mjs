/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["yt-krishna.s3.ap-south-1.amazonaws.com", "lh3.googleusercontent.com"]
    }
};

export default nextConfig;
